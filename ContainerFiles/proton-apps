FROM quay.io/fedora/fedora-toolbox:latest

LABEL com.github.containers.toolbox="true" \
      usage="This image is meant to be used with the toolbox or distrobox command" \
      summary="An example ContainerFile to demonstrate multiple image builds." \
      maintainer="ryanwalker8484@pm.me"

# Copy the setup scripts and package list
COPY ../scripts/proton-apps.sh /
COPY ../scripts/distrobox-shims.sh /
COPY ../packages/proton-apps.packages /

# Run the setup scripts
RUN chmod +x proton-apps.sh distrobox-shims.sh && /proton-apps.sh
RUN rm /proton-apps.sh /distrobox-shims.sh /proton-apps.packages
